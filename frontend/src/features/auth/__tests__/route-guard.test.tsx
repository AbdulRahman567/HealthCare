/**
 * @deprecated Prefer `@/features/authorization/__tests__/route-protection.test.tsx`.
 * RouteGuard is now an alias of RouteProtection.
 */
describe('RouteGuard (legacy alias)', () => {
  it('is covered by RouteProtection tests', () => {
    expect(true).toBe(true);
  });
});
