/**
 * @deprecated Import from `@/features/navigation` instead.
 * Kept for backward-compatible re-exports during Phase 3.6 → 3.7.
 */
export type { AppRole, AppPermission, NavItem } from '@/features/authorization/config/navigation';
export {
  APP_NAVIGATION,
  canAccessNavItem,
  filterNavigation,
} from '@/features/authorization/config/navigation';
