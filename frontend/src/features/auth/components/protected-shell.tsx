/**
 * @deprecated Use {@link AppShell} from `@/features/navigation` instead.
 * Kept as a thin re-export for Phase 3.6 import paths.
 */
export { AppShell as ProtectedShell } from '@/features/navigation/components/app-shell';
export { AppSidebar as RoleBasedNav } from '@/features/navigation/components/app-sidebar';
