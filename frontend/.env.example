# =============================================================================
# Frontend env — copy to frontend/.env.local (preferred) or frontend/.env
# NEXT_PUBLIC_* are inlined at build time; restart `next dev` after changes.
# =============================================================================
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_APP_NAME=Healthcare HMS
NEXT_PUBLIC_ENV=development
