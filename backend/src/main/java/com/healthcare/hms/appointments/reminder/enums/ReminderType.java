package com.healthcare.hms.appointments.reminder.enums;

/**
 * Semantic purpose of the reminder (extensible for Phase 11 follow-ups).
 */
public enum ReminderType {

    /** Reminder ahead of a booked appointment slot. */
    APPOINTMENT_UPCOMING
}
