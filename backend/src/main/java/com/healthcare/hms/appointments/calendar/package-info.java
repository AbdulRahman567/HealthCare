/**
 * Appointment calendar read models (Phase 6.5).
 *
 * <p>Optimized for scheduling UIs: paginated daily/weekly event loads with batch
 * label enrichment, and monthly day aggregates via a single {@code GROUP BY}.
 */
package com.healthcare.hms.appointments.calendar;
