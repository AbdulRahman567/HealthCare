/**
 * Department aggregate lives in {@link com.healthcare.hms.organization.entity.Department}
 * with API layers under {@code organization.controller / service / repository} (Phase 4.2).
 *
 * <p>This subpackage is retained as a documentation anchor for department-specific
 * extensions that should not leak into the shared organization root.
 */
package com.healthcare.hms.organization.department;
