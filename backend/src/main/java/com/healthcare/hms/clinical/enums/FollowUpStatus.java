package com.healthcare.hms.clinical.enums;

/**
 * Lifecycle of a follow-up plan originating from a consultation.
 */
public enum FollowUpStatus {

    PENDING,
    SCHEDULED,
    COMPLETED,
    CANCELLED,
    MISSED
}
