# Production / staging profile — activate with SPRING_PROFILES_ACTIVE=prod
# Requires real secrets via environment / secret manager (no change-me placeholders).

hms.security.jwt.allow-insecure-secrets=${JWT_ALLOW_INSECURE_SECRETS:false}
management.endpoint.health.show-details=when_authorized
spring.jpa.show-sql=false
